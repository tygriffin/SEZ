<ul class="study_tools">
  <%= content_tag(:li, "音声付き", :class => "includes_tool") unless a.audio.blank? %>
  <%= content_tag(:li, "文化説明", :class => "includes_tool") unless a.culture_notes.blank? %>
  <%= content_tag(:li, "単語付き", :class => "includes_tool") unless a.vocabulary_words.blank? %>
  <%= content_tag(:li, "クイズゲーム", :class => "includes_tool") unless a.quizzes.blank? %>
  <%= content_tag(:li, "学習ツールは準備中", :class => "includes_tool") if a.audio.blank? && a.culture_notes.blank? && a.vocabulary_words.blank? %>
</ul>
