<h1>Blog</h1>

<ul>

  <% @posts.each do |post| %>
    <li>
      <h2><%= post.title %></h2>
      <p><%= truncate(post.body, :length => 150, :ommision => "...") %></p>
      <span>Posted on <%= post.updated_at %></span>
    </li>
  <% end %>

</ul>
