.header-lead
  = image_tag "/images/bird-logo.png", :class => "bird-logo"
  %p 優しい英語で書いたウェブマガジン。単語の意味も辞書要らずのポップアップで簡単チェック。ボキャブラリーがどんどん増えます！

%section.catalog
  %ul
    - @articles.each do |article|
      = link_to article do
        %li.catalog-entry
          = image_tag article.image_url(:in_focus)
          .catalog-entry-info
            %h2
              %span.topic-marker #{article.topic.name}
              = article.title
            %p
              %i
                = "#{article.pubdate.strftime('%Y年%m月%d日')} 掲載　#{article.author.name} 著"  unless article.author.name == "SEZ"
            %p #{article.description}
            = render 'layouts/study_tools', :article => article

          = image_tag(add_flag(article.flag_message), :class => "attention-flag") unless article.flag_message.blank?
